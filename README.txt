🎮 Архитектура проекта:

    1. Frontend (Client Side):
        - HTML5 + JavaScript:
            ♦ Отвечает за отображение игрового интерфейса.
            ♦ Обеспечивает взаимодействие с пользователем (анимации, клики, ввод данных).
            ♦ Обменивается данными с сервером через AJAX-запросы (REST API).
            ♦ Поддерживает интеграцию с Telegram Web Apps (для использования в Telegram).

    2. Backend (Server Side):
        - ASP.NET Core Web API:
            ♦ Обработка запросов от фронтенда.
            ♦ Логика игры (механика слотов, расчёт выигрыша, генерация случайных значений).
            ♦ Авторизация и аутентификация пользователей (с использованием Telegram OAuth).

    3. Database:
        - MS SQL + Entity Framework Core:
            ♦ Хранение данных пользователей (ID, имя, баланс, история игр).
            ♦ Логирование результатов игр.
            ♦ Хранение настроек и конфигураций слотов.


🔄 Взаимодействие компонентов:

    1. Пользователь запускает игру через Telegram (Web App).
    2. Frontend (HTML5 + JS) отображает интерфейс и отправляет запросы на сервер (через AJAX/Fetch API).
    3. Backend (.NET Core API) обрабатывает запросы, выполняет бизнес-логику, взаимодействует с БД и возвращает результаты.
    4. Database (MS SQL) хранит и возвращает данные.


📂 Структура проекта:

    TelegramSlotGame
    │
    ├── Backend
    │   ├── TelegramSlotGame.API         // ASP.NET Core Web API
    │   │   ├── Controllers               // Контроллеры для API
    │   │   ├── Services                  // Логика игры (слоты, расчёты)
    │   │   ├── Models                    // Модели данных (DTO, ViewModels)
    │   │   ├── Data                      // DbContext и миграции EF Core
    │   │   └── Program.cs & Startup.cs   // Конфигурация API
    │   │
    │   └── TelegramSlotGame.Domain       // Доменные модели и бизнес-логика
    │       ├── Entities                  // Сущности БД
    │       ├── Repositories               // Репозитории для работы с данными
    │       └── Interfaces                // Интерфейсы для DI
    │
    └── Frontend
        ├── index.html                    // Главная страница игры
        ├── assets                        // Статические ресурсы (картинки, звуки)
        ├── css                           // Стили игры
        └── js
            ├── main.js                   // Основная логика игры (JS)
            ├── api.js                    // Запросы к API
            └── utils.js                  // Вспомогательные функции